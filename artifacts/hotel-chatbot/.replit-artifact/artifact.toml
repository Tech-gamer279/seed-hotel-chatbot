kind = "web"
previewPath = "/"
title = "Hotel Chatbot"
version = "1.0.0"
id = "artifacts/hotel-chatbot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20119

[services.development]
run = "pnpm --filter @workspace/hotel-chatbot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hotel-chatbot", "run", "build" ]
publicDir = "artifacts/hotel-chatbot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20119"
BASE_PATH = "/"
